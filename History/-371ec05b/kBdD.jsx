import React, { useContext } from 'react';
import Button from './Button'

const ButtonLanguage = () => {

	const icon = "X"

	return (
		<Button icon={icon} text={null} />
	);
};

export default ButtonLanguage;
